import React from 'react';

type DashboardType = {
  tags: any;
  expenses: any;
  categories: any;
};

export default function Container({
  tags,
  expenses,
  categories,
}: DashboardType) {
  return <div />;
}
