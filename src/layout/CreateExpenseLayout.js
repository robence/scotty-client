import React from 'react';

const CreateExpenseLayout = () => {
  return (
    <div>CreateExpenseLayout</div>
    // create form
    // amount, category, notes, tags, fromTemplate
  );
};
export default CreateExpenseLayout;
